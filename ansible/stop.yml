- name: lifecycle the openshift cluster
  hosts: localhost
  vars:
    # set the following to control the install/uninstall process
    silent: false
    become_override: false
    ACTION: "STOP" # START, STOP, UNINSTALL, UPDATE

  tasks:
    - name: gather cluster facts
      include_role:
        name: roles/gather-cluster-facts

    - name: stop the openshift cluster
      include_role:
        name: roles/lifecycle-cluster
